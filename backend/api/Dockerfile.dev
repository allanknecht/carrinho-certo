# Development: Rails server + PostgreSQL client, code mounted via compose
ARG RUBY_VERSION=3.3.7
FROM docker.io/library/ruby:${RUBY_VERSION}-slim

RUN apt-get update -qq && \
    apt-get install --no-install-recommends -y build-essential curl libpq-dev libyaml-dev postgresql-client && \
    rm -rf /var/lib/apt/lists /var/cache/apt/archives

WORKDIR /rails

ENV BUNDLE_PATH=/usr/local/bundle

COPY Gemfile Gemfile.lock ./
RUN bundle install

COPY . .

# Ensure binstubs are executable and use `ruby` instead of `ruby.exe`
RUN chmod +x bin/* && \
    sed -i "s/\r$//g" bin/* && \
    sed -i 's/ruby\.exe$/ruby/' bin/*

EXPOSE 3000

# Invoke bash explicitly so CRLF in shebang (Windows checkout) does not break kernel exec.
# db:create and db:migrate run on first start via bin/docker-entrypoint-dev (already in COPY . .)
ENTRYPOINT ["/bin/bash", "/rails/bin/docker-entrypoint-dev"]
CMD ["bin/rails", "server", "-b", "0.0.0.0"]
